#!/bin/bash

#
# My aliases
#
alias pd='pushd'
alias pu='pushd'
alias po='popd'
alias lr="ls -ltr"
alias ll='ls -l'
alias la='ls -A'
alias l='ls -CF'
alias dcommit='git svn dcommit'


# Places
alias idea='cd ~/Documents/IdeaProjects'

#
# Hosts
#

